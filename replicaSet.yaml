apiVersion: apps/v1
kind : ReplicaSet
metadata:
  name: nginx
spec:
  replicas: 3
  selector:
    matchLabels:
      batch: c38
  template:
    metadata:
      name: nginx
    spec:
      containers:
      - name: nginx
        image: nginx